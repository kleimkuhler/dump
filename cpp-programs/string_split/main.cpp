#include <iostream>
#include <string>
#include "split.h"

using std::cout;    using std::cin;
using std::endl;    using std::vector;
using std::string;  using std::getline;

int main() {
  string s;
  while (cin >> s)
    cout << s << endl;
  return 0;
}
